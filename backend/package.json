{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "pretest": "del-cli --force helpdesk-test.db && cross-env NODE_ENV=test node migrations/adicionarPrioridade.js && cross-env NODE_ENV=test node migrations/migrarChamados.js",
    "test": "cross-env NODE_ENV=test node index.js",
    "dev": "node --watch index.js"
  },
  "keywords": [],
  "dependencies": {
    "argon2": "^0.45.1",
    "better-sqlite3": "^13.0.3",
    "dotenv": "^17.4.2",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "del-cli": "^7.0.0"
  }
}
