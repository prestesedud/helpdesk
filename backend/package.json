{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "pretest": "del-cli --force helpdesk-test.db && node --env-file=.env.test migrations/adicionarPrioridade.js && node --env-file=.env.test migrations/migrarChamados.js",
    "test": "node --env-file=.env.test index.js",
    "dev": "node --env-file=.env.development --watch index.js",
    "start": "node --env-file=.env.production index.js"
  },
  "keywords": [],
  "dependencies": {
    "argon2": "^0.45.1",
    "better-sqlite3": "^13.0.3",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "del-cli": "^7.0.0"
  }
}
