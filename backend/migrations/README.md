#Migrations

Scripts para alterar a estrutura do banco de dados.
Rodar manualmente, em ordem, após o servidor já ter sido iniciado ao menos uma vez (para garantir que database.js já criou a tabela base).

Ordem de execução:

1. migrarChamados.js
2. adicionarPrioridade.js
